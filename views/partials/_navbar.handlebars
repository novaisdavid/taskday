 <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <div class="container">
                <a class="navbar-brand" href="/taskday">TASK DAY</a>
                <button
                    class="navbar-toggler"
                    type="button"
                    data-bs-toggle="collapse"
                    data-bs-target="#navbarSupportedContent"
                    aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                   <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0" >
                        {{#if user}}
                        <li class="nav-line">
                            <a class="nav-link active" aria-current="page" href="/taskday">taskday</a>
                        </li>
                        
                        <li class="nav-line">
                            <a class="nav-link active" aria-current="page" href="/taskday/criartarefa">Criar Tarefas</a>
                        </li>

                        <li class="nav-line">
                            <a class="nav-link active" aria-current="page" href="/taskday/vertarefascriadas">Tarefas Criadas</a>
                        </li>
                        {{else}}
                         <li class="nav-line">
                            <a class="nav-link active" aria-current="page" href="/taskday">taskday</a>
                        </li>
                        
                        {{/if}}
                           
                    </ul>
                    {{#if user}}
                        <i class="m-2 text-light">{{this}}</i>
                        <a class="btn btn-danger"  href="/taskday/logout">Sair</a>
                    {{else}}
                    <a class="btn btn-dark"  href="/taskday/login">login</a>
                    <a class="btn btn-info m-2"  href="/taskday/criarconta">Criar Conta</a>
                    {{/if}}
                </div>
            </div>

        </nav>